# LES Project Overlay

Copy only the templates a project chooses to own. LES installation does not
initialize or overwrite these paths. Replace instructional placeholders before
treating a file as active project policy.
