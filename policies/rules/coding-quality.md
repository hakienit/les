# Coding Quality

**Activate:** source changes.

Reuse existing patterns, then platform facilities, then existing dependencies.
Keep names domain-specific, control flow visible, and comments limited to intent
that code cannot express. Remove dead paths introduced by the change. New
abstractions require at least two real consumers or a proven boundary need.
