# Security

**Activate:** trust boundaries, credentials, sensitive data, permissions, or dependencies.

Validate untrusted input at entry, minimize privileges and retained data, keep
secrets out of files and output, and fail closed. Treat logs and generated
artifacts as disclosure surfaces. Security-relevant assumptions and unrun checks
must be explicit before completion.
