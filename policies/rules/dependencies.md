# Dependencies

**Activate:** adding, removing, or upgrading a dependency or runtime requirement.

Prefer the standard library and already-installed packages. Add a dependency only
when its maintained capability materially reduces owned risk or code. Pin through
the project's normal mechanism, assess license and security impact, and verify the
package boundary. Installation is a separate authorized action.
