# Git Safety

**Activate:** version-control operations.

Inspect status before edits and preserve unrelated work. Local reads and diffs
are R0; commits require user intent; push, history rewrite, force operations, and
destructive cleanup are R3. Use explicit targets and recoverable operations.
Never claim a commit or remote update that did not occur.
