# API Integration

Validate boundary data, separate transport from view state, cancel or ignore stale
responses, and expose recoverable failure. Mutations define pending, success,
failure, idempotency expectations, and cache reconciliation before completion.
