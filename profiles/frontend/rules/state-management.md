# State Management

Keep state at its authoritative owner. Derive values instead of synchronizing
copies, model async states explicitly, and make URL or server state authoritative
when users expect persistence or sharing. Introduce global state only for proven
cross-tree ownership.
